# Application
spring.application.name=smartContactManager
server.port=${PORT:8081}

# ===============================
# Database Configuration
# ===============================
# Use DATABASE_URL in proper JDBC format if available, else fallback to local
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/smartcontactmanager}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Mahir1234}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ===============================
# JWT Configuration
# ===============================
jwt.secret=${JWT_SECRET:U29tZVN1cGVyU2VjcmV0S2V5MTIzNDU2Nzg5MDEyMzQ1Ng==}
jwt.expirationMs=86400000

# ===============================
# Cloudinary Configuration
# ===============================
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dhq4or4qi}
cloudinary.api-key=${CLOUDINARY_API_KEY:263153189711768}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:CoyDBGx-qEH6vqmXwGLjb2X9nnE}

# ===============================
# Mail Configuration
# ===============================
mail.username=${MAIL_USERNAME:bajolemahir6@gmail.com}
mail.password=${MAIL_PASSWORD:pcwprlewlawlbeoa}
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true



